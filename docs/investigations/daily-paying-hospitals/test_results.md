# 测试结果

## 自动化测试

- 命令：`python -m unittest discover -s tests -v`
- 结果：40 项通过，0 项失败。

## 浏览器验证

- 数据：本地模拟 32 条医院聚合明细和 7 条每日汇总。
- 桌面视口：1440 x 1000。
- 桌面结果：双栏布局生效，页面无横向溢出，医院明细区域 `scrollHeight=1994`、`clientHeight=428`，可纵向滚动；控制台 0 条警告和错误。
- 桌面补充结果：1280 x 720 视口下每日汇总没有横向溢出，医院明细保持纵向滚动，页面没有横向溢出。
- 移动端视口：390 x 844。
- 移动端结果：统计卡片和两张表改为单列；网格宽度 325，面板宽度 351，页面没有横向溢出；每日汇总和医院明细均可在自身区域横向滚动，医院明细同时可纵向滚动；控制台 0 条警告和错误。
- 截图：`ui-verification-desktop.png`、`ui-verification-mobile.png`。

## 未执行项

- 生产数据库 SQL：本机缺少只读生产连接配置。
- 本地页面的既有统计明细接口依赖生产库，测试环境返回 500；浏览器验证聚焦本次新增的氪金医院区域，新增区域由本地模拟数据驱动，浏览器控制台没有错误。
