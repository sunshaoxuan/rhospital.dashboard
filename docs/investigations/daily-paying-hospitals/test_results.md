# 测试结果

## 自动化测试

- 命令：`python -m unittest discover -s tests -v`
- 结果：40 项通过，0 项失败。

## 浏览器验证

- 数据：本地模拟 5 条医院聚合明细和 2 条每日汇总，明细输入顺序刻意打乱。
- 桌面视口：1280 x 720。
- 桌面结果：左右滚动区域高度均为 430；默认选中 `2026-07-23`，点击 `2026-07-22` 后右侧从 3 条切换为该日 2 条，时间依次为 `18:10:00`、`08:05:00`；页面没有横向溢出，控制台 0 条警告和错误。
- 移动端视口：390 x 844。
- 移动端结果：统计卡片和两张表改为单列，两个滚动区域按内容自适应且最小高度均为 220；页面没有横向溢出，表格可在自身区域横向滚动；控制台 0 条警告和错误。
- 截图：`ui-verification-desktop.png`、`ui-verification-mobile.png`。

## 生产只读 SQL

- 执行位置：统计节点只读 PostgreSQL 连接。
- 结果：返回 34 条聚合明细，前三条最近支付时间为 `2026-07-23 16:21:10`、`11:51:10`、`01:33:43`，全量结果按最近支付时间倒序。

## 未执行项

- 本地页面的既有统计明细接口依赖生产库，测试环境返回 500；浏览器验证聚焦本次新增的氪金医院区域，新增区域由本地模拟数据驱动，浏览器控制台没有错误。
